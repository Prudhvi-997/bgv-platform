# Reverse proxy for the Client Portal.
# Per CLAUDE.md Part 4.5 — each portal is fronted independently.
# Candidate Portal is the public-internet-facing surface and is
# additionally WAF-fronted (not configured here).

server {
    listen 80;
    server_name client.kcheck.com;

    location / {
        root /var/www/client-portal;
        try_files $uri /index.html;
    }

    # Client Portal BFF — the ONLY backend surface this portal may reach.
    location /api/client/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
