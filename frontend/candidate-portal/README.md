# candidate-portal

Candidate Portal — mobile-first PWA, public-internet-facing.

**BFF target** — `/api/candidate/` only. This portal must not import
or call any other portal's BFF (CLAUDE.md Part 4.3).

## Dev

```
npm install
npm run dev   # serves on http://localhost:3002
```
