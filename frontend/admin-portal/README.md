# admin-portal

Super Admin Portal — KPMG platform governance.

**BFF target** — `/api/admin/` only. This portal must not import
or call any other portal's BFF (CLAUDE.md Part 4.3).

## Dev

```
npm install
npm run dev   # serves on http://localhost:3004
```
