// Public surface of the shared component library.
// Per CLAUDE.md, shared code is versioned — breaking changes here
// propagate to every portal and require a coordinated rollout.

export const SHARED_VERSION = "0.0.0";
